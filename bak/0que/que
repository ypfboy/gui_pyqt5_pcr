问题：
1、比如继承QComboBox类，并重写了， 如何在不修改相应ui文件或py文件的基础上使用重写后的类创建ui界面。
2、pyqt如何动态修改控件样式的一个属性，而不是重新绘制样式
3、如何设置背景图片充满容器控件
4、下拉框文字如何居中显示
5、如何使QTableWidget只显示横向网格线，不显示纵向网格线
6、如何在QTableWidget的一个单元格中存放两个控件或存放两段可以用来点击的文字， 如：修改/删除（可以分别点击修改和删除）
7、qtablewidget的横向表头添加复选框并设置居中
8、main文件中一下这一段代码绑定事件失败，为什么？
            # FIXME
            """
            for i in self.collect_buttons_obj():
                obj = eval(i)
                obj.clicked.connect(lambda:self.draw_pic(obj.objectName()))
            """
9、将fw_prc.py编译为pyd出错
    LINK :32 位链接器(D:\programs\vs\VC\Tools\MSVC\14.26.28801\bin\HostX86\x64\link.exe)用完了堆空间，正在重启与 64 位链接器的链接
    LINK :正在重启与 64 位链接器“D:\programs\vs\VC\Tools\MSVC\14.26.28801\bin\HostX64\x64\link.exe”的链接
    LINK : fatal error LNK1104: 无法打开文件“build\lib.win-amd64-3.7\views\fw_pcr.cp37-win_amd64.pyd”
    error: command 'D:\\programs\\vs\\VC\\Tools\\MSVC\\14.26.28801\\bin\\HostX86\\x64\\link.exe' failed with exit status 1104

10、将matplotlib版本从3.3.1降低到3.0.3，导致报如下错误
    Attribute Qt::AA_EnableHighDpiScaling must be set before QCoreApplication is created.
    Attribute Qt::AA_EnableHighDpiScaling must be set before QCoreApplication is created.


优化：
1、设置界面数据太多， 在切换全屏的时候会卡， 需要优化
2、uiTable中如何隔离复选框触发的itemchange事件
3、机器类型界面，如果数据太多， 启动会很慢， 并且删除时会重新创建表，数据也会很慢
4、试剂盒类型界面恢复默认设置时， 采用是先删除表，然后重新创建表， 重新写入表数据， 这里可以优化一下。
